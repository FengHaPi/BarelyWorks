# AI Video Studio v0.1.5 Alpha

发布日期：2026-08-25

## 修复内容

- 选择参考图后，权限勾选不再因系统文件窗口关闭后的前台刷新而消失。
- 文件选择控件不再在 `change` 事件到达前被卸载，上传请求能够正常开始。
- 普通前台同步不再让资产审核页或素材库闪回整页加载状态。
- 每张资产卡片会明确显示上传成功或失败；成功时展示文件名、参考类型与累计绑定数量。

此前未成功的选择不会被伪装成已上传，也不会留下半成品记录。用户需要重新选择一次原参考图；服务端仍会完整执行格式、尺寸、像素解码、SHA256 和权限校验。
