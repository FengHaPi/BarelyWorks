# Output contract

Return JSON matching `screenplaySchema` in `src/shared/skill-schemas.ts`.

`basedOnApprovedArtifact` identifies the exact source version or hash. `sourcePreserved` is true only when no unapproved semantic rewrite occurred. Actions remain playable rather than abstract, and questions remain separate from canonical scene content.
