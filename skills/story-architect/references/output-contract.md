# Output contract

Return JSON matching `storyOutlineSchema` in `src/shared/skill-schemas.ts`.

Each sequence needs a dramatic purpose, concrete events, and an estimated duration. `lockedFacts` records source truths. `proposedChanges` is always explicit and separable from the outline, so the user can reject it without losing the draft.
