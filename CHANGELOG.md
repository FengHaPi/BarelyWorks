# 更新日志

本项目的重要变化记录在此。版本格式参考 [Semantic Versioning](https://semver.org/lang/zh-CN/)，变更分类参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [Unreleased]

## [0.2.0] - 2026-08-28

### Agent-first 原地重构

- 用独立 Head、artifact 血缘、版本审批和项目工作区替代全局闯关状态作为新界面的唯一事实来源；历史版本和兼容入口保留。
- 批准只记录目标版本决定，不再自动生成下一步、自动切换 Head、自动提交付费平台或自动创建粗剪。
- 项目 Agent 的询问、比较、修改和影响分析全部调用真实 Codex 执行器；Provider 失败、网络错误、超时和取消不会回退固定成功文案。
- 新增持久化 Operation 与事件日志，为 Agent、素材清单、镜头包、收件箱导入、连续性修复和粗剪提供幂等、进度、刷新恢复和取消。
- 新增项目问题中心，将问题定位到责任产物并显示证据、修复顺序和显式修订入口；忽略问题不改变产物或绕过门禁。
- 生成、质检、粗剪和交付能力迁入 Agent-first 工作区，恢复逐镜头包、人工导入、九维质检和交付审核的完整操作链。

### 累计核查与检查器可信度

- 新增 `cumulative-verification-v1`：进入任一后续环节时，从原始输入开始重新检查全部适用上游。
- 累计核对 Head、批准证据、文件哈希、结构合同、递归血缘、资产/镜头覆盖范围、连续性报告和生产记录。
- 确定性规则、模型 + 版本化 Skill、人工批准作为独立检查主体记录来源、版本和健康状态。
- 检查器缺失、报告解析失败或来源绑定无法证明时记录为未知并阻断相应门禁，不再把“没有结果”解释成通过。
- 连续性问题映射到最早责任产物，并按资产定义、导演脚本、分镜的依赖顺序生成修复计划。

### 参考图角色链路

- 视觉资产参考图角色升级为服务端共享契约，按人物、场景、道具、服装和风格限制可选角色；音频资产不再暴露图片上传入口。
- H3 预检根据是否存在有效参考图明确选择 T2VA 或 Ref2VA，保留每张图片的实际角色并阻止无角色或不兼容角色。
- 主参考、正面、侧面、背面、表情、服装和其他参考分别生成职责约束，并进入模型输入、提示词优化、主体定义、保留分析和交付清单。
- 参考图新增不覆盖旧文件，替换和删除先归档；浏览器与服务端统一校验 PNG/JPEG/WebP、大小、像素解码、授权与项目路径。

### 付费生成安全门禁

- 新增大纲/剧本容量分析，按剧情 Beat、高风险任务、5 秒最低镜头时长计算建议镜头数与最低可靠总时长；容量不足时禁止继续批准和生成导演脚本。
- 项目可将目标时长调整到建议值；操作保留全部历史文件和审批记录，并要求相关产物重新建立可证明的当前版本与依赖关系。
- 新增逐镜头确定性复杂度预算：最多 4 个主要可见 Beat、3 段运镜、6 个精确事件门和 2 层高风险任务；屏幕、复杂镜面、多复制体群体和反常空间不再无限叠加。
- 文字连续性、视频模型可执行性和真实成片视觉验收分开记录；结构检查通过不再被展示为实片已验证。
- 分镜只有同时通过结构一致性和模型可执行性检查才能批准；H3 生成前再次执行同一硬门禁。
- 提示词结构、字符预算、参考标签重复度或模型可执行性失败时最多内部重试三次，仍失败则不保存问题版本。
- 执行策略升级为 `model-executability-v2`；旧 H3 投递包自动过期，避免继续复制或投递旧高风险提示词。

### 生成链路重构

- Codex CLI 文字生成固定到 `AI_VIDEO_STUDIO_CODEX_MODEL` 指定的型号，默认 `gpt-5.6-sol`；健康页会显示当前真实型号。
- H3 Skill 按模式只加载 `base-en.txt` 或 `ref-en.txt` 中的一份，不再同时注入互斥指南。
- 导演脚本、分镜和 H3 只接收压缩后的必要资产信息；H3 输入改为确定性执行简报，不再重复传入完整 ShotSpec、Storyboard 和资产大对象。
- 执行简报只保留可见动作、对白与声音、连续性、引用素材、物理关系和复杂度报告，减少重复引用与上下文遗忘。

### 导演脚本与分镜底层规则

- 导演脚本升级为 `shooting-script-v2`，每个新镜头必须提供结构化 `physicalPlan`，分别描述现实/屏幕内/仅镜面存在的实体实例、连续摄影机视点、身体/头部/视线朝向、显示面关系、反射拓扑和延迟事件门。
- 单面显示设备处于正常阅读状态时必须朝向使用者；摄影机读屏只能采用匹配的越肩、侧角、插入、反射或剧情明确展示机位，不能再为了让镜头看见而把手机屏幕异常朝外。
- 正常镜像、镜面独有实体和现实实体必须分别计数并使用不同实例；镜面异常需要保留反射面边界与现实空间证据。
- 灯光闪烁、故障、实体出现和声音变化等延迟事件增加“首次允许发生时间”，在该时间前必须保持明确的前置状态。
- 分镜升级为 `storyboard-v2`，逐镜头核验摄影机调度、显示面几何、反射拓扑和事件门；失败项会合并进入连续性报告并阻止通过。
- H3 投递前再次执行确定性物理预检；旧项目仍可读取并保持现有数据不变，但会提示重新生成导演脚本与分镜后才能获得新硬校验。
- 导演脚本与 H3 投递统一采用整数秒生产规则：新项目最低 5 秒，每个镜头必须为 5–15 的整数秒，15 秒项目使用 8+7、9+6 或 5+5+5 等分配，不再生成 7.5+7.5。
- H3 投递包新增来源 ShotSpec 指纹和目标时长绑定；导演脚本变化后，旧提示词包会明确标为历史失效版本，禁止继续复制素材、复制提示词或标记投递，避免标题时长与实际提示词不一致。
- 生成中心在页面顶部直接显示时长迁移入口，并逐镜头对比当前时长和历史投递时长；旧文件保留，不做静默覆盖或四舍五入。

### 验证

- `npm run check` 通过 TypeScript 类型检查、服务端/UI 测试、前后端生产构建和真实浏览器 E2E。
- 33 个服务端测试文件共 154 项测试通过；14 个 UI 测试文件共 47 项测试通过；1 条 Agent-first 浏览器 E2E 通过。
- 本地浏览器实测累计核查、问题中心修复路径、检查器健康状态、参考图角色选择和音频资产隔离，控制台无错误。

## [0.1.6] - 2026-08-25

### 新增

- 新增 `asset-reference-prompt-writer` Skill，把单个人物、场景、道具、服装、风格或参考资产编译为等义的中英文参考图提示词、负面提示词、构图要求和连续性锁定项。
- 每个视觉资产增加“Codex 生成提示词”“复制全部提示词”和“生成参考图”入口；提示词按资产版本保存到本地项目并保留生成追踪。
- 新增独立 `ImageGenerationProvider` 接口、能力查询和参考图生成 API；默认 Provider 明确处于未配置/关闭状态，不调用付费图像服务。
- 已配置 Provider 返回的图片仍需经过 MIME、完整像素解码、大小、项目路径和 SHA256 校验后才能绑定资产。

### 交互调整

- 人物和场景的视觉摘要、完整外观、固定识别特征、禁止漂移与连续性规则不再被“简洁流程”隐藏。
- 上传自有参考图的权限确认与系统生成参考图分开；未配置图片 API 时按钮明确显示“API 待接入”，不会把 Codex 文本输出伪装成图片。

## [0.1.5] - 2026-08-25

### 修复

- 修复系统文件选择窗口关闭时触发前台刷新，导致资产面板被卸载、参考图上传事件丢失以及权限勾选自动消失的问题。
- 资产审核页和素材库在普通前台同步时不再切回整页加载状态；选择文件期间会跳过多余的焦点刷新，同时保留跨标签页数据同步。
- 参考图上传成功后在对应资产卡片内显示文件名、参考类型和已绑定图片数量；失败时就地显示具体错误，不再只剩无响应的按钮。
- 增加文件选择器焦点刷新回归测试，并保留服务端 MIME、像素解码、尺寸、SHA256 与授权校验。

## [0.1.4] - 2026-08-25

### 修复

- 新建项目弹窗不再因点击周围遮罩而关闭，避免长篇故事、风格和项目参数被意外清空。
- 新建项目表单会自动保存到当前浏览器会话；使用关闭按钮、Esc 或“暂存并关闭”退出后，重新打开仍恢复全部字段和换行内容。
- 新增明确的“清空草稿”操作并要求二次确认；项目创建成功后才清除会话草稿，创建失败时输入保持不变。
- 补充损坏草稿、版本不兼容、时长越界和多行正文恢复测试。

## [0.1.3] - 2026-08-25

本次 Alpha 增量发布加入“安全一键推进”，把确定性、可恢复的本地步骤串联起来，同时保留内容质量、外部视频生产和最终交付三类必要人工门禁。

### 一键流程

- 大纲、剧本、资产和导演脚本批准后，直接调用真实本地 Skill 生成下一阶段草案；生成后停在人工审核，不会自动批准内容。
- 最终分镜批准后，自动完成素材锁定和 Updream 初始化包创建，并直接进入生成中心；中途失败会保留已完成步骤并给出可继续操作。
- 生成中心新增“一键准备生成”和“一键生成全部缺失镜头包”；仅处理预检通过且尚无包的镜头，已有版本不会被重复生成或覆盖。
- 批量镜头包按项目写队列逐个执行，显示当前镜头与总进度；单个失败不会抹掉其他成功结果，预检失败镜头会明确跳过。
- 最后一个镜头通过快速或高级审核后，自动创建本地粗剪并进入成片终审；媒体工具不完整或粗剪失败时，审核结果仍安全保留。

### 交互简化

- 移除逐镜头包、手动粗剪和最终交付按钮之后的重复浏览器确认框；按钮本身保留明确动作名称与结果说明。
- 外部 Updream 视频生成仍保持人工操作，程序不会擅自调用付费接口、上传私人素材或替用户判断画面质量。

## [0.1.2] - 2026-08-25

本次 Alpha 增量发布重构整个本地工作台的信息层级：正常生产只显示当前内容、状态和唯一下一步；问题诊断、版本历史和专业审核进入二级处理，同时完成真实双镜头导回与快速质检验证。

### 交互与可读性

- 默认启用大字号“简洁流程”，正文、按钮、表单、状态和镜头信息整体放大；不再以 7–10px 小字承载主要操作。
- 顶部新增“发现问题 / 详细处理”入口；原文、版本对比、哈希、Skill 来源、媒体能力、九维证据、驳回意见和历史记录统一放入详细模式。
- 页面或请求发生错误时自动进入详细处理，保证诊断信息不会被简洁模式隐藏。
- 项目总览默认只保留当前工作与主要动作；文字阶段默认只显示当前版本和批准按钮；生成、质检与交付页面隐藏非必要技术面板。
- 质量审核新增一键通过和问题标签；只有选择人物、动作、场景、运镜、多余文字、水印、画质或声音问题时才生成结构化重试要求。
- 全部镜头通过后，始终显示“创建粗剪并进入成片终审”，不再把下一步藏在高级审核中。
- 成片批准后自动退出旧审核界面并进入独立“项目交付完成”起始页；提供创建新项目、下载最终成片、查看交付文件和查看已完成项目，历史数据保持原样。

### 修复

- 修复分镜模型漏写 `characterIds`、`sceneId` 或 `requiredAssetIds` 时，明明存在的已批准资产被误报为缺失的问题；人物、场景、道具和风格引用现在由已批准导演脚本确定性继承，真正的未知资产仍会被拦截。
- 连续性报告在分镜生成后立即展示具体错误、警告、影响范围和修复建议；未通过时批准按钮预先锁定，驳回仍可正常操作。
- 项目画幅现作为资产生成硬参数传入，风格资产声明冲突画幅会在资产审批阶段被拦截，不再拖到分镜阶段才暴露。
- 连续性报告新增“定点修复”链路：只修改报告点名的资产与 ShotSpec，逐阶段另存、人工批准，最后只更新受影响分镜并重新检查；未受影响对象和全部历史版本保持不变。
- 当前资产投影只读取最新 Asset Bible 的成员，旧版本已删除资产不再形成“幽灵资产”；只有身份与造型指纹兼容的同 ID 资产才继承本地参考图、哈希、角色和授权状态。
- 成片画幅检查与定点修复只处理场景/风格中的最终构图描述，区分否定约束、人物设定图和场景内手机/显示器比例；同时校验项目画幅与输出分辨率方向一致。
- 参考图使用 Sharp 完整解码 PNG/JPEG/WebP 像素，并校验 PNG chunk/CRC/IEND、MIME、最小尺寸和解码像素上限；界面要求用户主动确认图片使用权限。
- 生成视频导入增加镜头时长、项目画幅和最低 480p 门禁，粗剪前重新校验已接受视频哈希。
- 同一项目的全部写操作和后台收件箱扫描使用进程内互斥，重复请求返回明确冲突，不再并发启动多个 Codex/媒体任务；资产和镜头投影改为 SQLite 事务写入。
- 收件箱复扫按文件哈希保持幂等，确定性规格失败采用短时缓存且完整原因在界面展示；手机旋转元数据会在画幅门禁前归一为显示尺寸。
- Codex JSONL 改为流式累计线程与用量信息，长输出不再因 32K 诊断尾部截断而丢失线程 ID。
- 前端 API 增加超时、取消、非 JSON 错误解析和“提交成功但刷新失败”分离提示，并补充对话框键盘操作和关键错误可访问性。
- 流程侧栏改为显示真实阶段进度并移除伪按钮语义；多标签页切回前台时刷新项目、健康与工作区快照，丢弃乱序响应；文本和镜头编辑增加 expected-latest 乐观锁，会话草稿保留创建时的版本基线，避免恢复旧草稿后覆盖新版本。
- 首页不再把读取失败伪装成 0，并显示真实交付版本数；提交期间冻结相关草稿输入，避免响应刷新覆盖用户刚输入的内容。
- 生成、质检和交付工作区首屏读取失败时显示具体错误，并提供页内重试与返回总览入口，不再永久停留在加载动画。
- 生成中心把“逐镜头生成 H3 投递包”提升为第 3 步首要操作，增加醒目的下一步和编译中状态；收件箱扫描移到镜头包之后，零镜头包时不再误导性提前开放，并按当前 ShotSpec 显示正确文件名。
- H3 镜头包按钮和版本路径明确显示 S001/S002 镜头编号，并注明每个镜头独立从 V001 计数，避免把不同镜头的同版本号误认为重复内容。

### 安全与稳定性

- Updream 镜头 ID 和包版本统一使用严格路径组件校验；手工版本来源必须存在、属于当前项目且类型一致。
- 浏览器来源仅允许实际 API 端口和开发 UI 的受信任本地地址；后台扫描异常会记录且不会形成未处理拒绝，关闭服务时等待在途扫描。
- Provider Skill 锁文件加入内容 SHA-256，内容与固定版本不一致时拒绝加载。
- FFmpeg 安装强制要求合法 SHA-256，校验通过前不执行压缩包内容。
- 应用、插件和本地 Skill 版本统一为 `0.1.2`；Node 最低版本与 Vite 对齐到 `22.12.0`。

### 验证

- 新增后端与前端隔离回归测试，并将两组测试纳入 `npm test`；新增 Windows GitHub Actions 类型检查、测试和生产构建门禁。真实 FFmpeg 与 Phase 5 验收继续由本地 `verify:v1` 执行。
- 新增全项目代码审查报告，记录本轮已修复问题、仍需专项处理的数据一致性风险、性能与架构债务。

### 计划中

- 使用真实生成视频完成导回、质量审核、粗剪和最终交付验收。

## [0.1.1] - 2026-08-25

本次 Alpha 增量发布完成本地媒体链路与 V1 发布门禁，并补齐项目归档、480p 输出、关键帧质检及交付下载。真实供应端视频质量仍需人工验收，付费视频 API 继续保持关闭。

### 新增

- 新增可恢复的项目删除、已归档项目列表与恢复入口。
- SQLite 以增量迁移方式增加项目归档时间；归档时保留原始内容、素材、生成记录和全部历史版本。
- 新增媒体工具链预检：自动发现项目便携 FFmpeg 目录，并验证 ffmpeg、ffprobe、libx264 与 AAC 能力。
- 生成中心展示可复制的便携目录和逐项媒体能力状态；粗剪仅在四项能力全部通过后启用。
- 新增本地 FFmpeg ZIP 安装脚本：支持 SHA-256 校验、编码器预检、旧版本备份和失败回滚，不自动联网下载。
- 新增真实媒体工具链自检：合成有声/无声测试片段，调用正式粗剪实现并用 ffprobe 验证 H.264、AAC、分辨率和时长。
- 已安装项目便携 FFmpeg 9.0.1 Essentials Build；ffmpeg、ffprobe、libx264、AAC 与正式合成媒体粗剪均已通过验证。
- 成片交付页新增 MP4、SRT 和 Markdown 报告下载；下载接口只允许读取当前项目内已记录的交付文件。
- 新增 Phase 5 真实端到端验收脚本：临时创建项目，串联收件箱导入、ffprobe、九维审核、FFmpeg 粗剪、SRT、终审交付和三类文件下载。
- 导入视频时使用 FFmpeg 自动抽取起始、中段和结束三张质检关键帧；抽帧失败不会伪造结果或阻断视频归档。
- 九维审核界面新增三点关键帧对照，并通过项目内安全读取接口展示。
- 新增核心 Skill 契约 EVAL 和 `verify:v1` 发布门禁，覆盖缺失信息、冲突约束、长文本保真与质检证据规则。
- 成片输出规格新增横版、竖版和方形 480p 档位，并在服务端拒绝低于 480px 短边的输出。
- 项目输出规格与供应端生成清晰度正式解耦：480p/720p/768p/1080p 在逐镜头投递时选择，只进入设置和人工清单，不进入任何 Codex/H3 提示词。

### 验证

- 10 个 Vitest 测试文件、43 项测试全部通过。
- TypeScript 类型检查、服务端构建和 React/Vite 生产构建通过。
- `verify:media` 已验证有声/无声测试片段、H.264/AAC 编码、探测和正式粗剪。
- `verify:phase5` 已验证收件箱导入、三点关键帧、九维审核、1080p 粗剪、SRT、终审和三类文件下载。

### 安全

- FFmpeg 可执行文件、运行时项目、SQLite、日志、参考素材和生成媒体均保持在 Git 忽略范围内。
- 本地服务继续仅监听 `127.0.0.1`，付费视频 API 继续默认关闭。

## [0.1.0] - 2026-08-25

### 新增

- Windows 本地优先的 React/Vite + Fastify + SQLite/Drizzle 项目骨架。
- 输入、大纲、剧本、资产、导演脚本、分镜、生成、质检和交付九阶段工作流。
- 不可变原始文件、结构化产物版本、内容哈希、批准记录和下游失效机制。
- 9 个项目生产 Skill，以及固定版本的 `h3-prompt-writing` 和 `updream-handoff` Skill。
- 真实 Codex CLI 结构化生成，记录运行 ID、线程、用量、事件类型、Skill 来源和耗时。
- 素材库、稳定资产 ID、镜头引用、参考图上传、本地预览和制作就绪门禁。
- “原创完整设定”和“忠于已有文本／参考图”两种资产设计模式。
- 结构化 ShotSpec 编辑器、时间码校验、分镜生成和连续性检查。
- H3 参数预检、Updream 初始化包、逐镜头版本包与人工上传状态。
- 质量审核数据模型、审核维度、生成版本记录和本地媒体工具链。

### 修复

- 修复重新生成、批准、驳回和另存为新版本缺少实际状态变化的问题。
- 修复资产定义只复述“尚未确定”、人物卡片缺少可制作视觉设定的问题。
- 旧版空壳资产即使曾被批准，也会被新的制作就绪规则拦截。
- Codex 生成从固定 5 分钟改为按产物类型设置超时；资产定义最长等待 12 分钟。
- 超时后主动终止任务，保留运行诊断和可能存在的部分输出，不改变项目数据。
- 页面生成状态显示已等待时间、预计耗时和最长等待上限，避免重复提交。

### 验证

- 9 个 Vitest 测试文件、31 项测试全部通过。
- TypeScript 类型检查、服务端构建和 React/Vite 生产构建通过。
- 新资产定义 V002 在 269.2 秒内完成，15/15 项资产通过制作就绪校验。

### 安全

- 本地服务仅绑定 `127.0.0.1`。
- 付费视频 API 默认关闭。
- `.env`、SQLite、项目内容、参考图、生成媒体和运行日志全部排除在 Git 之外。
